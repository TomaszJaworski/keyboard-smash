/**
 * Helper function returning layout of 'qwerty' keyboard.
 * Each value is responsible for one row of rendered keyboard
 */
export const getKeyboardLayout = function (): string[] {
    // TODO: add space, enter, shifts to layout
    return ['1234567890-=', 'qwertyuiop[]', 'asdfghjkl;"', 'zxcvbnm,./'];
};
